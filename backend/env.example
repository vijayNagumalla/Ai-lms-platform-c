# Database Configuration
# For MySQL (legacy)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_secure_password_here
DB_NAME=lms_platform
DB_PORT=3306

# Supabase Configuration (for deployment)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT Configuration
JWT_SECRET=your_secure_jwt_secret_key_minimum_32_characters_long
JWT_EXPIRES_IN=7d

# CSRF Protection
CSRF_SECRET=4c052540c317693173dbd2856a1a6c0d38ea8219a95705e12447826aac5e2a66

# Encryption Key (REQUIRED - minimum 32 characters)
ENCRYPTION_KEY=b38580412fa3139b2aa1ae36c9d593b067ba277a9704bcb7d6123aac788a5602

# Server Configuration
PORT=5000
NODE_ENV=development

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Super Admin Registration
# WARNING: Change this code in production! This is just an example.
# Generate a secure random code (minimum 32 characters recommended)
SUPER_ADMIN_REGISTRATION_CODE=69f33325a72b5863b2c1859d3a34f1f0b23376584c3f6d54afc0e03925ae547c

# Judge0 Configuration (for coding questions)
JUDGE0_URL=http://localhost:2358
JUDGE0_API_KEY=your_judge0_api_key_here

# Code Execution Configuration
CODE_EXECUTION_TIMEOUT_MS=5000
# Timeout in milliseconds for code execution (default: 5000ms / 5 seconds)
# Increase for complex algorithms, decrease for faster execution

CODE_EXECUTION_MEMORY_LIMIT=128m
# Memory limit for Docker containers (default: 128m)
# Increase for memory-intensive algorithms

DOCKER_CONTAINER_POOLING=false
# Enable Docker container pooling for faster code execution (default: false)

DOCKER_MAX_POOL_SIZE=3
# Maximum number of containers in pool (default: 3)

# Email Configuration (for notifications)
# IMPORTANT: For Gmail, you MUST use an App Password, not your regular password
# Steps to set up Gmail App Password:
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Go to Google Account settings > Security > App passwords
# 3. Generate a new app password for "Mail"
# 4. Use that 16-character password (without spaces) as SMTP_PASS
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-gmail-address@gmail.com
SMTP_PASS=your-16-character-app-password
SMTP_FROM=noreply@lms-platform.com

# Frontend URL (REQUIRED in production)
# For local development:
FRONTEND_URL=http://localhost:5173
# For production (comma-separated for multiple origins):
# FRONTEND_URL=https://your-frontend-domain.com,https://www.your-frontend-domain.com

# API URL (for Swagger documentation)
# API_URL=https://your-api-domain.com

# Email Rate Limiting
EMAIL_RATE_LIMIT=10
# Number of emails per rateDelta (default: 10)

EMAIL_RATE_DELTA=20000
# Time window in milliseconds (default: 20000ms / 20 seconds)

EMAIL_BATCH_SIZE=10
# Number of emails to process in each batch (default: 10)

EMAIL_BATCH_DELAY=2000
# Delay between batches in milliseconds (default: 2000ms / 2 seconds)

# Database Connection Pool
DB_CONNECTION_LIMIT=10
# Maximum number of database connections (default: 10 for development, 50 for production)

DB_POOL_MONITORING_INTERVAL=30000
# Connection pool monitoring interval in milliseconds (default: 30000ms / 30 seconds)

# Proctoring Configuration
PROCTORING_DATA_RETENTION_DAYS=90
# Number of days to retain proctoring data (default: 90)

PROCTORING_TAB_SWITCH_PER_MIN=3
# Maximum tab switches per minute before violation (default: 3)

PROCTORING_TAB_SWITCH_PER_SESSION=20
# Maximum tab switches per session before violation (default: 20)

PROCTORING_COPY_PASTE_PER_MIN=2
# Maximum copy/paste actions per minute before violation (default: 2)

PROCTORING_COPY_PASTE_PER_SESSION=10
# Maximum copy/paste actions per session before violation (default: 10)

PROCTORING_RIGHT_CLICK_PER_MIN=5
# Maximum right-clicks per minute before violation (default: 5)

PROCTORING_RIGHT_CLICK_PER_SESSION=30
# Maximum right-clicks per session before violation (default: 30)

# Attendance Configuration
ATTENDANCE_QR_EXPIRY_MINUTES=15
# QR code expiration time in minutes (default: 15)

ATTENDANCE_QR_ONE_TIME_USE=true
# Enable one-time use for QR codes (default: true)

ATTENDANCE_GPS_ACCURACY_THRESHOLD=50.0
# GPS accuracy threshold in meters (default: 50.0)

# Export Configuration
EXPORT_MAX_RECORDS=10000
# Maximum number of records per export (default: 10000)

EXPORT_MAX_FILE_SIZE=52428800
# Maximum export file size in bytes (default: 52428800 / 50MB)

EXPORT_BATCH_SIZE=1000
# Number of records to process in each batch (default: 1000)

EXPORT_CLEANUP_INTERVAL_HOURS=6
# Interval between cleanup jobs in hours (default: 6)

EXPORT_FILE_TTL_HOURS=24
# Time-to-live for export files in hours (default: 24)

# Analytics Configuration
ANALYTICS_CACHE_TTL=300000
# Cache TTL in milliseconds (default: 300000ms / 5 minutes)

ANALYTICS_MAX_CACHE_SIZE=100
# Maximum number of cached entries (default: 100)

ANALYTICS_DEFAULT_PAGE_SIZE=50
# Default page size for analytics queries (default: 50)

ANALYTICS_MAX_PAGE_SIZE=100
# Maximum page size for analytics queries (default: 100)

# Security Configuration
PASSWORD_MIN_LENGTH=8
# Minimum password length (default: 8)

CSRF_TOKEN_EXPIRY=3600000
# CSRF token expiration in milliseconds (default: 3600000ms / 1 hour) 

# Gemini AI Configuration
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-1.5-flash-latest
GEMINI_TEMPERATURE=0.35
GEMINI_MAX_OUTPUT_TOKENS=1024
